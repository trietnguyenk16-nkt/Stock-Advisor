# UI picker preview findings — 2026-08-16

The local preview renders the redesigned dashboard successfully. The AI section now shows `AI LENS`, `READY/SETUP`, `Phân tích đầu tư`, selected model, readiness/configuration state, and the current asset count. The watchlist still initially loads local cached assets and quote status; the picker interaction itself requires opening `Thêm mã`. The production domain may still show an older build until the latest UI checkpoint is redeployed.
